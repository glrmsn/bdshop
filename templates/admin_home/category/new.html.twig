<!DOCTYPE html>

<title>New Category</title>

{% block body %}
    <h1>Create new Category</h1>

    {{ include('admin_home/category/_form.html.twig') }}

    <a href="{{ path('category_index') }}">back to list</a>
{% endblock %}
